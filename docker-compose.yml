version: '3.8'

services:
  bot:
    build: .
    container_name: senpai-waifu-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=Asia/Kolkata
    volumes:
      - ./logs:/app/logs
